/**
 * Ads Module
 *
 * Handles ads mode resolution and configuration.
 */

export { AdsModeResolver } from './AdsModeResolver';
export { AdsMode } from './types';
export type { AdsModeResolution, AdsConfig, IAdsModeResolver } from './types';
